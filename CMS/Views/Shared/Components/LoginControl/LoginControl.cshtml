@model User

@if (Model.Id == 0)
{
    <h4> @Html.ActionLink("Login", "Login", "Home")</h4>
}
else
{
    <h3>Welcome, @Model.Username</h3>
    <h4> @Html.ActionLink("Logout", "Logout", "Home")</h4>
}
