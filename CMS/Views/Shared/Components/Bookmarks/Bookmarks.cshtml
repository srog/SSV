@model Dictionary<string, string>

<div>
    @foreach (var bookmark in Model)
    {
        <div class="col-md-1" >
            <a class="button large" target="_blank" href=@bookmark.Value>@bookmark.Key</a>
        </div>
    }
</div>