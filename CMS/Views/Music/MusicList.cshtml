@model IEnumerable<CMS.Models.MusicEntry>

@{
    ViewData["Title"] = "Music List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h2>Music List</h2>


<table class="table">
    <thead>
        <tr>

            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
               (Average Rating)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
    
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            
            <td>
                @Component.InvokeAsync("MusicRating", new { entityId = item.Id }).Result

            </td>
            
            <td>
                @Component.InvokeAsync("MusicRater", new { entityId = item.Id }).Result
            </td>
        </tr>
}
    </tbody>
</table>
